import { AutoCompleteEntity } from './auto-complete-entity';

describe('AutoCompleteEntity', () => {
  it('should create an instance', () => {
    expect(new AutoCompleteEntity()).toBeTruthy();
  });
});
